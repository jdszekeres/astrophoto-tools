{
  "name": "astronomy-tools",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild js/*.js js/components/*.js css/*.css sw.js --minify --minify-identifiers --tree-shaking=true --allow-overwrite --outdir=. --outbase=."
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
